name: Pipeline-rust

on:
    workflow_dispatch:
    push:
        paths:
            - "**.rs"
            - "ai/**"
    pull_request:
        paths:
            - "**.rs"
            - "ai/**"

jobs:
    build:
        name: Build
        uses: ./.github/workflows/rust-ci.yml
    coding-style:
        name: Coding-style
        uses: ./.github/workflows/coding-style.yml
